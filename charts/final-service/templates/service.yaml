apiVersion: v1
kind: Service
metadata:
  name: {{ include "final-service.fullname" . }}
  labels:
    {{- include "final-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "final-service.selectorLabels" . | nindent 4 }} 